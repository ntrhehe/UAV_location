# 为什么不用四角 + 仿射了？

## 下视（pitch ≈ -90°）时为什么能用？

下视时：图像和地面**近似平行**。

```
相机
  ↓  (垂直向下)
  ┌──────┐
  │ 图像 │  投影到地面 → 矩形（或近似矩形）
  └──────┘
```

4 个角点 → 地面 4 个点 → 仿射变换（平移/旋转/缩放/剪切）。

仿射变换**刚好够用**，因为矩形 → 矩形（或平行四边形）没有透视变形。

## 倾斜时为什么不行？

pitch ≠ -90° 时：图像和地面**不平行**，投影后是**梯形**。

```
相机  ← 斜着拍
    ↙
  ┌──────┐
  │ 图像 │  投影到地面 → 梯形
  └──────┘
```

- 近端像素覆盖地面小（高分辨率）
- 远端像素覆盖地面大（低分辨率）

仿射变换只能做**线性**变换（平行线仍是平行线），但梯形包含了**透视**变形（平行线不再平行）——仿射管不了。

## GCP 网格 + Warp 怎么解决？

```
┌──┬──┬──┬──┐
├──┼──┼──┼──┤  每个交点是 GCP，都经过精确的射线+DSM 求交
├──┼──┼──┼──┤
└──┴──┴──┴──┘
```

1. 单个仿射不够 → 用**成百上千个点**约束映射
2. GDAL 用这些点做 Thin Plate Spline 或多项式插值 → 每个像素映射到正确位置
3. 重采样输出最终正射 GeoTIFF

一句话：**4 个点只够描述线性变形，倾斜视角的透视需要 ≥ 二阶模型，GCP 网格提供了足够约束。**
